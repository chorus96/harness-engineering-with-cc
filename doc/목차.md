# 하네스엔지니어링 with Claude Code — 목차

> 출판: 한빛미디어 · [도서 페이지](https://www.hanbit.co.kr/store/books/look.php?p_code=B2817272480) (도서코드 B2817272480)
>
> 목차의 밑줄 친 용어를 클릭하면 문서 하단 [용어 설명](#용어-설명)의 해당 항목으로 바로 이동합니다.
> 각 챕터 아래 **📎 예제로 보기** 블록은, 그 절이 설명하려는 개념을 **본 저장소의 예제 코드**로 어떻게 확인할 수 있는지 정리한 것입니다(예제 상세 분석은 [`예제코드.md`](./예제코드.md) 참고).

---

## Part 01 — 하네스란 무엇인가

### Chapter 01 · 왜 하네스인가
- 스스로 데이터를 지운 단일 에이전트
- 혼자 일하는 AI는 자기 실수를 못 본다
- 모델의 환경을 바꿨을 때 일어난 일들
- 병목은 능력이 아니라 구조다
- [하네스](#g-harness) = [around the model](#g-around-model)
- [하네스](#g-harness)에 대한 세 가지 오해

> 📎 **예제로 보기**
> - **병목은 구조다 / around the model** → [`ex-02-01-ab-comparison`](../ex-02-01-ab-comparison). 모델·프롬프트를 그대로 두고 **주변 구조(에이전트+스킬)만** 얹었을 때 산출물이 달라지는 것을 A/B로 실측 — "능력이 아니라 구조가 병목"이라는 1장 주장을 뒷받침하는 실험.
> - **혼자 일하는 AI는 자기 실수를 못 본다** → [`ex-02-02-my-first-harness`](../ex-02-02-my-first-harness)의 author↔reviewer 분리. 생성자와 별도의 검증자를 두어 "스스로 못 보는 실수"를 잡는 최소 구조.

### Chapter 02 · 30분 Quick Start
- [Step 1] 같은 프롬프트, 다른 산출물
- [Step 2] 30분 안에 2인 팀 만들기
- 자주 틀리는 세 가지

> 📎 **예제로 보기**
> - **[Step 1] 같은 프롬프트, 다른 산출물** → [`ex-02-01-ab-comparison`](../ex-02-01-ab-comparison). 동일한 한 줄 프롬프트(`claude code 사용 팁 정리해주세요`)를 맨몸(`non-harness/`) vs 하네스(`apply-harness/`)로 실행: 자유 형식 치트시트 1개 ↔ "상황→방법→예시→주의점" 표준 포맷 8편+인덱스. `index.html`에서 2단 비교.
> - **[Step 2] 30분 안에 2인 팀 만들기** → [`ex-02-02-my-first-harness`](../ex-02-02-my-first-harness). `commit-message` 스킬 1개 + `commit-msg-author`/`commit-msg-reviewer` 2 에이전트로 구성한 최소 하네스. 스테이지된 diff를 받아 Conventional Commits 메시지를 협업 생성.

---

## Part 02 — 하네스 구성하기: 에이전트·스킬·오케스트레이터

### Chapter 03 · [에이전트](#g-agent)·[스킬](#g-skill)·[오케스트레이터](#g-orchestrator)의 책임 분리
- 2인 팀을 분해해보자
- 한 파일에 다 넣으면 무엇이 깨지는가
- 세 가지 핵심 요소
- 세 가지 핵심 요소를 파일로 확인하기
- 세 가지 핵심 요소는 어떻게 동작하는가
- 세 가지 핵심 요소가 섞이면 생기는 문제

> 📎 **예제로 보기**
> - **세 가지 핵심 요소를 파일로 확인하기** → [`ex-02-02-my-first-harness`](../ex-02-02-my-first-harness)가 세 요소를 서로 다른 파일로 물리적으로 분리한 실물: 규칙은 `CLAUDE.md`, 오케스트레이션(순차 호출·PASS/REDO 분기)은 `.claude/skills/commit-message/SKILL.md`, 실무는 `.claude/agents/commit-msg-{author,reviewer}.md`. "한 파일에 다 넣으면 깨진다"의 반례로 읽을 수 있음.

### Chapter 04 · [에이전트](#g-agent)를 정의한다는 것
- 어제 잘 굴러가던 팀이 오늘 아침 사라지는 이유
- 본문 7개 섹션 — 왜 필요하고 어떻게 쓰는가
- [가드레일](#g-guardrail) — 에이전트가 할 수 없는 것을 파일로 정하기
- [프론트매터](#g-frontmatter) 다섯 필드와 16개 공식 필드
- 핵심 파일 읽기 — copy-editor.md
- 책 집필 하네스의 7개 에이전트 관계도
- 안티패턴

> 📎 **예제로 보기**
> - **가드레일 — 할 수 없는 것을 파일로** → [`ex-04-01-security-analyst`](../ex-04-01-security-analyst). 프론트매터 `tools: Read, Grep, Glob, Bash`에서 **`Write`/`Edit`를 의도적으로 제외**해 "읽기 전용"을 물리적으로 강제. Bash도 조회용으로만 쓰도록 규칙으로 재차 좁힘.
> - **프론트매터 다섯 필드** → 두 예제의 프론트매터 대비: `security-analyst.md`는 `model: sonnet` + `tools` 명시, `copy-editor.md`는 `tools` **미기재로 부모 도구 상속**(주석에 근거 기록). 필드를 넣고/빼는 선택이 그대로 권한이 됨.
> - **핵심 파일 읽기 — copy-editor.md** → [`ex-04-02-copy-editor`](../ex-04-02-copy-editor)의 `.claude/agents/copy-editor.md`가 본문 섹션 구조(핵심 역할·처리 범위·작업 원칙·입출력 프로토콜·에러 핸들링·협업·품질 자체 검증)를 완비. "무엇을 고치고(콜론·Bold 조사·오탈자 3종) 무엇은 안 건드리는지(예외 케이스)"로 경계를 명시.

### Chapter 05 · [스킬](#g-skill) 설계의 기술
- 스킬을 썼는데 왜 호출이 안 될까
- 원리 1 — [Description](#g-description)이 호출을 결정한다
- 원리 2 — [Progressive Disclosure](#g-progressive) 3단계
- 원리 3 — [Why-First](#g-whyfirst) 원칙
- 핵심 파일 읽기 — book-writer/SKILL.md
- 구현 — book-writer 스킬을 펼치다
- 스킬 검증 — With/Without 비교
- 안티패턴

> 📎 **예제로 보기**
> - **원리 1 — Description이 호출을 결정** → [`ex-05-05-separation-signals`](../ex-05-05-separation-signals)와 각 SKILL.md의 `description`("~를 언급하면 호출. 단, ~는 아니다" 식). 어떤 스킬이든 description이 트리거 판단의 근거임을 보여줌.
> - **원리 2 — Progressive Disclosure** → [`ex-05-06-domain-references`](../ex-05-06-domain-references). `sql-query/SKILL.md`는 본문 없이 라우터(조건부 링크)만 두고, 도메인 본문은 `references/{finance,inventory}.md`로 위임해 **필요할 때만 로드**. 호출 트레이스 2건으로 조건부 로드를 시각화.
> - **원리 3 — Why-First** → [`ex-05-11-with-without`](../ex-05-11-with-without)의 `csv-summary/SKILL.md`: "숫자 열만 통계를 낸다 — **왜냐하면** 문자열 열에 통계는 의미가 없기 때문" 처럼 규칙마다 이유를 병기. [`ex-05-09-context-savings`](../ex-05-09-context-savings)는 이유를 남기며 산문을 30%+ 가지치기.
> - **핵심 파일 읽기 / 구현 — book-writer** → [`ex-05-10-book-writer-skill`](../ex-05-10-book-writer-skill). 두 버전 프론트매터 diff + 3원리(Pushy·Layer·Why-First) 매핑, 그리고 실제 `mini-multi-mode` 스킬(워크플로 3종)로 "펼치는" 구현.
> - **스킬 검증 — With/Without** → [`ex-05-11-with-without`](../ex-05-11-with-without). 같은 스킬을 With/Without로 **동시 실행**(`&`+`wait`)해 `pass_rate`·`duration_ms`·`total_tokens` 3축 비교, 변별력 없는 단언의 반례까지 산출.
> - **안티패턴** → [`ex-05-12-antipatterns`](../ex-05-12-antipatterns)(거대 SKILL.md/references 부재/이유 없는 규칙 3종 진단) + [`ex-05-08-generalization`](../ex-05-08-generalization)(오버피팅 vs 일반화 적용률 비교).

### Chapter 06 · [오케스트레이터](#g-orchestrator)
- 3인이 넘으면 리더가 무너진다
- 리더는 [이벤트 루프](#g-eventloop), 팀원은 피어
- 세 가지 기본 도구 — 팀을 데이터 전달 방식으로 보기
- [TeamCreate](#g-teamcreate) — 팀은 상수가 아니라 세션 객체
- [TaskCreate](#g-taskcreate) — 공유 작업 큐와 [depends_on](#g-dependson)
- [SendMessage](#g-sendmessage) — 리더를 거치지 않는 [peer-to-peer](#g-p2p) 채널
- 에이전트 파일의 [팀 통신 프로토콜](#g-protocol) 섹션
- 리더의 역할
- 데이터 흐름
- 실전 예제 — [PR 리뷰](#g-pr) 오케스트레이터 SKILL.md
- 292개 에이전트 사건
- [TeamDelete](#g-teamdelete)와 정리 — 세션당 한 팀, shutdown 필수
- 언제 팀 프리미티브를 도입할까
- 관측과 경계면 — 팀을 바깥에서 보기
- 안티패턴

> 📎 **예제로 보기**
> - **실전 예제 — PR 리뷰 오케스트레이터 SKILL.md** → [`ex-06-12-pr-review-skill-md`](../ex-06-12-pr-review-skill-md)의 `pr-review-orchestrator/SKILL.md`가 Phase 0~5 전문을 담음: `TeamCreate`로 팀 생성 → 팀원 4인 → `TaskCreate` 4작업.
> - **TaskCreate와 depends_on** → 위 SKILL.md의 `integrate-review` 작업이 `depends_on = ["static-pass","security-pass","test-pass"]`로 앞 3작업 완료 후 실행.
> - **SendMessage / peer-to-peer / 팀 통신 프로토콜** → 같은 SKILL.md Phase 3이 "리더 미경유" 팀원 간 메시지를 규정하고, 메시지 형식을 JSON 스키마(`type/severity/file/line/claim/request`)로 **고정**.
> - **리더는 이벤트 루프 / 리더의 역할** → 오케스트레이터는 코드를 직접 고치지 않고 팀 생성·작업 배치·통합만. [`ex-11-05-orchestrator-6phase`](../ex-11-05-orchestrator-6phase)의 "리더 무발화" 원칙도 동일.
> - **TeamDelete와 정리** → 위 SKILL.md Phase 5가 `shutdown_request` → `TeamDelete`(단, `_workspace/`는 보존) 순서를 명시.
> - **292개 에이전트 사건** → [`ex-06-15-292-agent-incident`](../ex-06-15-292-agent-incident): 채널 수 폭주 사례를 정량 데이터+외부 자료로 교차 검증(무분별한 에이전트 증식의 비용).

---

## Part 03 — 하네스 스킬로 구성하기

### Chapter 07 · [메타 하네스 스킬](#g-metaskill)과 [6단계 파이프라인](#g-pipeline6)
- 여섯 번째 팀을 만들던 날
- [메타스킬](#g-metaskill)이란 무엇인가
- [메타스킬](#g-metaskill)이 여는 [6단계 파이프라인](#g-pipeline6)
- [Phase](#g-phase) 1. 도메인 분석 — 질문을 정제하는 단계
- [Phase](#g-phase) 2. 팀 아키텍처 설계
- [Phase](#g-phase) 3·4. 에이전트와 스킬 — 파일이 없으면 존재하지 않는다
- [Phase](#g-phase) 5. 오케스트레이션
- [Phase](#g-phase) 6. 검증
- [Phase](#g-phase) 0·7 — 순환 구조와 메타 하네스 루프
- 안전장치 — 메타스킬도 폭주할 수 있다
- 안티패턴

> 📎 **예제로 보기**
> - **메타스킬의 산출물** → [`ex-02-01-ab-comparison`](../ex-02-01-ab-comparison)의 `apply-harness/`는 `/harness` 메타 스킬로 생성된 하네스(전용 에이전트 3종 + 공유 스킬 3종 + `CLAUDE.md` 포인터). 메타스킬이 6단계로 "무엇을 뽑아내는지"의 실물 예.
> - **Phase 3·4 — 파일이 없으면 존재하지 않는다** → [`ex-07-13-phase3-agent-5sections`](../ex-07-13-phase3-agent-5sections). `security-reviewer.md`를 8섹션 완비로 작성하고 "5필수 vs 7~8섹션"을 비교 — 에이전트는 파일로 완성돼야 존재함을 보여줌.
> - **안전장치 — 폭주할 수 있다** → [`ex-08-15-max-retries-code`](../ex-08-15-max-retries-code)의 `MAX_RETRIES` 루프처럼, 자동 반복에는 재시도 상한과 에스컬레이션이 필요.

### Chapter 08 · 6가지 아키텍처 패턴
- 팀에는 모양이 있다
- 에이전트를 몇 명으로 쪼갤 것인가
- 패턴 1. [파이프라인](#g-pat-pipeline) — 순서가 의미를 만든다
- 패턴 2. [팬아웃·팬인](#g-pat-fan) — 같은 입력, 다른 관점
- 패턴 3. [전문가 풀](#g-pat-expert) — 라우터 + 전문가
- 패턴 4. [생성-검증](#g-pat-genver) — 만들고 검사하고 다시 만든다
- 패턴 5. [감독자](#g-pat-supervisor) — 런타임 동적 분배
- 패턴 6. [계층적 위임](#g-pat-hier) — 총괄 → 팀장 → 실무자
- 복합 패턴은 실전의 기본값
- 패턴 선택 기준 — 플로우차트와 전환 신호
- 전통 분산 패턴과의 대응
- 안티패턴

> 📎 **예제로 보기**
> - **패턴 1. 파이프라인** → [`ex-02-02-my-first-harness`](../ex-02-02-my-first-harness)(author→reviewer 순차), [`ex-13-01-dependency-mapper`](../ex-13-01-dependency-mapper)(수집→import 추출→클러스터링→위상정렬의 순차 절차).
> - **패턴 2. 팬아웃·팬인** → [`ex-11-06-jwt-pr-bugs`](../ex-11-06-jwt-pr-bugs)/[`ex-06-12-pr-review-skill-md`](../ex-06-12-pr-review-skill-md)(같은 diff를 4 리뷰어에 병렬로 뿌리고 하나로 통합), [`ex-12-03-team-8-agents`](../ex-12-03-team-8-agents) Phase 1의 api/ui/db 삼각 팬아웃.
> - **패턴 3. 전문가 풀 (라우터+전문가)** → [`ex-05-06-domain-references`](../ex-05-06-domain-references)의 `sql-query` 라우터가 요청을 매출/재고 전문 references로 분기.
> - **패턴 4. 생성-검증** → [`ex-08-15-max-retries-code`](../ex-08-15-max-retries-code)(generator/verifier + `MAX_RETRIES=3` 후 escalate), [`ex-02-02`](../ex-02-02-my-first-harness)(author/reviewer REDO 루프 2회 상한), [`ex-11-04-three-frontmatter`](../ex-11-04-three-frontmatter)의 `refactorer`(생성-검증 3회 상한).
> - **패턴 6. 계층적 위임** → [`ex-12-03-team-8-agents`](../ex-12-03-team-8-agents)의 `feature-pm`(총괄) → 워커 → `test-suite`(서브에이전트) 위임 구조.

### Chapter 09 · 팀 / [서브에이전트](#g-subagent) / [하이브리드](#g-hybrid)
- 팀을 짤까, 그냥 각자 시킬까
- 실행 모드별 트레이드오프
- 실행 모드 결정
- 아키텍처 패턴과 실행 모드
- 구현 — 세 템플릿 골격
- [예시] 제품 런칭 준비 하네스
- 안티패턴

> 📎 **예제로 보기**
> - **팀 vs 서브에이전트** → [`ex-12-03-team-8-agents`](../ex-12-03-team-8-agents)가 두 모드를 한 하네스에서 대비: Phase 1~3의 워커들은 `TeamCreate`로 묶인 **팀**(상호 `SendMessage`), `test-suite`는 팀에 넣지 않고 PM이 `AgentTool`로 직접 부르는 **서브에이전트**(`01_phase_matrix.md`에 "PM 단독 수신"으로 명시). 언제 팀이고 언제 서브에이전트인지의 실물 기준.

### Chapter 10 · 하네스 등록과 진화
- 하네스는 진화하는 생명체다
- 포인터는 4줄, 시스템은 4층
- 하네스 등록 — [CLAUDE.md](#g-claudemd) 템플릿과 실전용 변형
- 진화의 두 관문 — Phase 0 현황 감사와 7-5 운영 루프
- 피드백 반영과 변경 이력 — [drift](#g-drift)를 방지하는 물리적 증거
- [재실행과 부분 재실행](#g-rerun)
- 심화 — [ADR](#g-adr)과 [Build to Delete](#g-btd)
- 안티패턴
- 하네스 등록과 진화 통합 체크리스트

> 📎 **예제로 보기**
> - **하네스 등록 — CLAUDE.md 포인터** → 여러 예제의 루트 `CLAUDE.md`가 "규칙 + 무엇이 어디에 있는지"를 담은 포인터 실물: [`ex-02-02`](../ex-02-02-my-first-harness/CLAUDE.md)(규칙 3줄), [`ex-12-03`](../ex-12-03-team-8-agents)·[`ex-13-01`](../ex-13-01-dependency-mapper) 등.
> - **재실행 / 부분 재실행** → [`ex-11-05-orchestrator-6phase`](../ex-11-05-orchestrator-6phase)의 트리거("리뷰 재실행", "다시 실행")가 동일 PR을 같은 팀으로 재돌리는 재실행 진입점.

---

## Part 04 — 하네스 실전편

### Chapter 11 · 코드 리뷰 자동화 팀
- 시나리오 — [PR 리뷰](#g-pr)
- 팀 구성
- 워크플로
- 핵심 파일 훑어보기
- 실행 결과
- 응용 가이드

> 📎 **예제로 보기** (11장은 예제가 가장 촘촘합니다)
> - **팀 구성** → [`ex-11-03-static-analyzer-md`](../ex-11-03-static-analyzer-md)(`static-analyzer` 실물)와 [`ex-11-04-three-frontmatter`](../ex-11-04-three-frontmatter)(security/design/refactor + static 4인). 도구 경계를 frontmatter로 강제: `design-reviewer`는 **Bash 없음**, `refactorer`는 **Edit 단독**.
> - **워크플로** → [`ex-11-05-orchestrator-6phase`](../ex-11-05-orchestrator-6phase)의 `code-review-team/SKILL.md`가 6 Phase 오케스트레이션(의사코드)과 "공식 5종 도구 vs 의사 함수 4종" 분류를 정리.
> - **실행 결과** → [`ex-11-06-jwt-pr-bugs`](../ex-11-06-jwt-pr-bugs). SQL 인젝션·N+1·경계면 불일치·테스트 0건 4버그를 심은 JWT PR에 4 리뷰어를 적용해 4×4 발견 매트릭스와 Without/With 비교 산출.
> - **응용 가이드** → [`ex-14-10-application-guide`](../ex-14-10-application-guide)의 4개 `application-*.md`.

### Chapter 12 · 풀스택 기능 구현 팀
- 시나리오 — 로그인 기능 추가
- 팀 구성
- 워크플로
- 핵심 파일 훑어보기
- 실행 결과
- 응용 가이드

> 📎 **예제로 보기**
> - **팀 구성 / 핵심 파일** → [`ex-12-03-team-8-agents`](../ex-12-03-team-8-agents)의 8 에이전트(feature-pm/api/ui/db/backend/frontend/boundary/test) 실물. 설계·검증은 `opus`, 구현·테스트는 `sonnet`으로 모델 티어링, 도구도 역할별로 경계.
> - **워크플로 (Phase)** → `01_phase_matrix.md`의 Phase×에이전트 활성 매트릭스: 동시 활성 ≤ 4명(워커 3 + PM), 초과 시 세션 분할. `boundary-verifier`는 7 경계면 패턴을 교차 검증하고 PASS/FIX/REDO 판정만.

### Chapter 13 · 레거시 [마이그레이션](#g-migration) 팀
- 시나리오 — 3,500개의 레거시 테스트 파일
- 팀 구성
- 워크플로
- 핵심 파일 훑어보기
- 응용 가이드

> 📎 **예제로 보기**
> - **핵심 파일 / 워크플로** → [`ex-13-01-dependency-mapper`](../ex-13-01-dependency-mapper). `dependency-mapper`(Read/Grep/Bash만)가 `sample-src/` import 그래프를 분석 → 복잡도 = (size/100)+(imports×2)+(functions×3) → **위상정렬** → 마이그레이션 배치 계획을 `batches.json`으로 산출(순환은 한 배치로 묶고 `warning:"cyclic"`). 기본 mock(비용 0), `USE_LIVE_LLM=1`에서 실제 호출. 대규모 레거시를 결정론적 배치로 쪼개는 실물.

### Chapter 14 · 디버깅 / [RCA](#g-rca) 팀
- 시나리오 — "로컬에선 잘 되는데요"
- 팀 구성
- 워크플로
- 핵심 파일 훑어보기
- 응용 가이드
- 마무리 — 스케일별 하네스 진화

> 📎 **예제로 보기**
> - **응용 가이드 / 핵심 파일** → [`ex-14-10-application-guide`](../ex-14-10-application-guide). Post-mortem·플래키·성능·1인 개발 4개 `application-*.md`와 `reviewer.md`(Post-mortem 전용). `reviewer`는 **`tools: Read` 단독**으로 사건 산출물 전체를 [blameless](#g-blameless) 관점으로만 검토(수정·실행 권한 없음) — RCA 팀의 읽기 전용 검토자 실물.

---

## 부록

- **[부록 A]** 클로드 코드 설치·환경 설정
- **[부록 B]** 안티패턴 카탈로그 + 도구 설계 패턴
- **[부록 C]** 트러블슈팅 — 자주 막히는 실패 패턴 20선
- **[부록 D]** [토큰 경제](#g-token)와 [레퍼런스 아키텍처](#g-refarch)

> 📎 **예제로 보기**
> - **토큰 경제** → [`ex-05-06-domain-references`](../ex-05-06-domain-references)(references 조건부 로드로 상시 컨텍스트 절약), [`ex-05-09-context-savings`](../ex-05-09-context-savings)(30%+ 가지치기), [`ex-11-06-jwt-pr-bugs`](../ex-11-06-jwt-pr-bugs)(live 모드 입력·출력 5k 토큰 상한).

---

## 스킬(skill) 부연 설명

> 목차 5장 「[스킬](#g-skill) 설계의 기술」과 관련 항목을 처음 접하는 독자를 위해, **스킬이 무엇이고 어떻게 생겼는지**를 본 저장소 예제 기준으로 자세히 풀었습니다.

### 스킬이란 — 에이전트와의 차이

**스킬(skill)** 은 자연어 요청에 반응해 호출되는 **재사용 가능한 워크플로**입니다. [에이전트](#g-agent)가 "누가(역할·권한을 가진 일꾼)"라면, 스킬은 "무엇을 어떤 순서로(절차)"에 가깝습니다.

| 구분 | 에이전트(agent) | 스킬(skill) |
|---|---|---|
| 위치 | `.claude/agents/<이름>.md` | `.claude/skills/<이름>/SKILL.md` |
| 성격 | 역할·도구·모델이 고정된 **일꾼** | 여러 단계를 담은 **절차/레시피** |
| 트리거 | 오케스트레이터가 호출 | `description`에 걸린 **자연어 요청**이 직접 호출 |
| 예 | `commit-msg-author`, `security-analyst` | `commit-message`, `sql-query`, `csv-summary` |

### 파일 구조

스킬은 폴더 하나로 이루어지며, 본문(`SKILL.md`)에 더해 보조 파일을 둘 수 있습니다.

```
.claude/skills/<이름>/
├── SKILL.md            ← 프론트매터 + 본문(절차)   ← 항상 로드
├── references/         ← 도메인 상세 (조건부 로드)   예) sql-query
│   ├── finance.md
│   └── inventory.md
├── workflows/          ← 모드별 세부 절차           예) mini-multi-mode
│   └── 01-readme.md
└── bin/                ← 실제 실행 스크립트          예) csv-summary
    └── run.sh
```

- **`references/`** — SKILL.md 본문은 얇게 두고 상세는 여기로 위임해, **필요할 때만 로드**합니다([Progressive Disclosure](#g-progressive)). → [`ex-05-06-domain-references`](../ex-05-06-domain-references)
- **`bin/`** — 스킬이 말만 하는 게 아니라 **실제로 동작**하는 구현을 담습니다. → [`ex-05-11-with-without`](../ex-05-11-with-without)의 `csv-summary/bin/run.sh`(파이썬 stdlib로 CSV 통계 산출)
- **`workflows/`** — 하나의 스킬이 여러 모드를 가질 때 모드별 절차를 분리. → [`ex-05-10-book-writer-skill`](../ex-05-10-book-writer-skill)의 `mini-multi-mode`

### 프론트매터 3필드

```yaml
---
name: commit-message
description: "스테이지된 변경을 바탕으로 커밋 메시지를 2인 팀으로 생성. '커밋 메시지' 요청 시 반드시 사용. 단, 이미 메시지가 있는 경우는 아니다."
allowed-tools: Bash Read Write
---
```

- **`name`** — 스킬 식별자.
- **`description`** — **호출 여부를 결정하는 가장 중요한 필드.** "무엇을·언제 쓰고, 무엇은 **아닌지**"까지 적을수록 오호출이 줄어듭니다(→ [원리 1](#g-description)).
- **`allowed-tools`** — 이 스킬이 쓸 수 있는 도구 화이트리스트. 에이전트의 `tools`처럼 [가드레일](#g-guardrail) 역할을 합니다.

### 스킬 설계 3원리 (5장 핵심)

1. **[Description이 호출을 결정한다](#g-description)** — 스킬이 안 불리는 1순위 원인은 description이 모호해서입니다. 트리거 키워드와 제외 조건을 명시합니다.
2. **[Progressive Disclosure 3단계](#g-progressive)** — ① 프론트매터(요약) → ② SKILL.md 본문(절차·라우팅) → ③ `references/`(도메인 상세). 앞 단계만으로 판단하고, 깊은 정보는 필요할 때만 펼쳐 [토큰](#g-token)을 아낍니다.
3. **[Why-First](#g-whyfirst)** — 규칙마다 "왜"를 붙입니다. 예) [`ex-05-11`](../ex-05-11-with-without) `csv-summary`: *"숫자 열만 통계를 낸다 — 왜냐하면 문자열 열에 통계는 의미가 없기 때문."*

### 저장소 예제로 보는 스킬의 세 유형

| 유형 | 하는 일 | 예제 |
|---|---|---|
| **오케스트레이션 스킬** | 여러 에이전트를 순차·병렬 호출하고 결과를 통합 | [`commit-message`](../ex-02-02-my-first-harness)(author→reviewer), [`pr-review-orchestrator`](../ex-06-12-pr-review-skill-md), [`code-review-team`](../ex-11-05-orchestrator-6phase) |
| **라우터 스킬** | 본문은 얇게, 요청을 조건부로 `references`에 분기 | [`sql-query`](../ex-05-06-domain-references)(매출→finance, 재고→inventory) |
| **실행 스킬** | `bin/`의 실제 스크립트로 산출물을 직접 생성 | [`csv-summary`](../ex-05-11-with-without)(CSV 통계 요약) |

### 스킬 검증과 안티패턴

- **With/Without 검증** — 스킬이 정말 효과가 있는지 같은 작업을 스킬 유무로 실측 비교. → [`ex-05-11-with-without`](../ex-05-11-with-without)(`pass_rate`·`duration_ms`·`total_tokens` 3축).
- **안티패턴 3종** — ① 거대 SKILL.md(모든 걸 본문에), ② `references` 부재(도메인 분기를 본문에 욱여넣음), ③ 이유 없는 규칙(Why 누락). → [`ex-05-12-antipatterns`](../ex-05-12-antipatterns)가 이 3종을 진단·심각도 라벨링.

---

## 용어 설명

> 목차에 등장하는 전문용어를 처음 접하는 독자를 위해 풀어 정리했습니다. 책 본문의 정의가 아니라, 용어가 가리키는 개념을 일반적인 의미로 설명한 것입니다. 각 항목 끝의 [↑ 목차](#하네스엔지니어링-with-claude-code--목차)로 위로 돌아갈 수 있습니다.

### 큰 개념

<a id="g-harness"></a>
- **하네스(harness)** — 말·낙하산의 "마구·장비"에서 온 말로, 여기서는 **AI 모델을 감싸는 작업 환경 전체**를 뜻합니다. 프롬프트 한 번을 잘 던지는 것이 아니라, 모델이 일하기 전에 에이전트·스킬·규칙·도구 경계를 미리 세팅해두는 구조를 가리킵니다.

<a id="g-around-model"></a>
- **around the model** — "모델 자체(성능)를 바꾸는 것이 아니라 모델 **주변**을 설계한다"는 하네스의 슬로건. 병목이 모델 능력이 아니라 그것을 둘러싼 구조에 있다는 관점입니다.

<a id="g-agent"></a>
- **에이전트(agent)** — 특정 역할·도구·모델로 **고정된 작업 단위**. Claude Code에서는 `.claude/agents/<이름>.md` 파일 하나가 에이전트 하나이며, 파일이 없으면 그 에이전트는 "존재하지 않는" 것으로 취급됩니다.

<a id="g-skill"></a>
- **스킬(skill)** — 자연어 트리거로 호출되는 **재사용 워크플로**. `.claude/skills/<이름>/SKILL.md`에 정의하며, 여러 단계를 담은 절차나 여러 에이전트를 순차·병렬로 호출하는 흐름을 담습니다. → 📖 자세히: [스킬 부연 설명](#스킬skill-부연-설명)

<a id="g-orchestrator"></a>
- **오케스트레이터(orchestrator)** — 여러 에이전트로 이루어진 **팀을 지휘하는 주체**. 직접 결과물을 쓰기보다 팀을 만들고, 작업을 배치하고, 결과를 통합하는 "지휘자" 역할을 합니다.

<a id="g-subagent"></a>
- **서브에이전트(subagent)** — 상위 에이전트가 필요할 때 **직접 호출하는 하위 에이전트**. 팀(공유 큐·상호 통신)에 편입되지 않고 단발성으로 불려 쓰이는 실행 모드입니다.

<a id="g-hybrid"></a>
- **하이브리드(hybrid)** — 팀 방식과 서브에이전트 방식을 **섞어 쓰는 실행 모드**.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 에이전트·스킬 파일 구성

<a id="g-frontmatter"></a>
- **프론트매터(frontmatter)** — 마크다운 파일 맨 위에 `---`로 감싸 넣는 **메타데이터 블록**. `name`·`description`·`model`·`tools` 같은 필드로 에이전트/스킬의 정체성과 권한을 선언합니다.

<a id="g-guardrail"></a>
- **가드레일(guardrail)** — 에이전트가 **할 수 없는 일을 파일로 못박아 두는 안전 경계**. 예를 들어 `tools`에서 `Write`/`Edit`를 빼면 "수정 불가(읽기 전용)"가 물리적으로 강제됩니다.

<a id="g-description"></a>
- **Description** — 프론트매터의 설명 필드. Claude가 **언제 이 스킬/에이전트를 호출할지 판단하는 근거**가 되므로, "무엇을·언제·무엇은 아닌지"를 명확히 쓸수록 호출 정확도가 올라갑니다. → 📖 자세히: [스킬 설계 3원리](#스킬skill-부연-설명)

<a id="g-progressive"></a>
- **Progressive Disclosure(점진적 공개)** — 필요한 정보만 단계적으로 드러내는 설계 원칙. SKILL.md 본문은 **라우터(요약·조건부 링크)** 로 얇게 두고, 상세 내용은 `references/`로 위임해 **필요할 때만 로드**함으로써 컨텍스트를 절약합니다. → 📖 자세히: [스킬 설계 3원리](#스킬skill-부연-설명)

<a id="g-whyfirst"></a>
- **Why-First 원칙** — 규칙마다 **"왜 그렇게 해야 하는지" 이유를 함께 적는** 설계 습관. 이유가 있으면 모델이 규칙의 의도를 이해해 예외 상황에서도 올바르게 판단합니다. → 📖 자세히: [스킬 설계 3원리](#스킬skill-부연-설명)

<a id="g-claudemd"></a>
- **CLAUDE.md** — 프로젝트 루트에 두는 **규칙·오케스트레이션 포인터 파일**. 팀 전체가 지켜야 할 공통 규칙과 "무엇이 어디에 있는지" 안내를 담습니다.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 오케스트레이션·팀 도구

<a id="g-teamcreate"></a>
- **TeamCreate** — **팀(세션 객체)을 생성**하는 도구. 팀은 고정 상수가 아니라 세션 동안만 존재하는 실행 단위입니다.

<a id="g-taskcreate"></a>
- **TaskCreate** — 팀의 **공유 작업 큐에 작업을 등록**하는 도구.

<a id="g-dependson"></a>
- **depends_on** — 한 작업이 **다른 작업들의 완료를 선행 조건으로 요구**하는 의존 관계 선언. 예: 통합 작업은 앞선 3개 리뷰가 끝나야 시작.

<a id="g-sendmessage"></a>
- **SendMessage** — **에이전트끼리 리더를 거치지 않고 직접 주고받는 메시지 채널**(peer-to-peer). 팀원 간 발견 사항 공유에 쓰입니다.

<a id="g-teamdelete"></a>
- **TeamDelete** — 작업이 끝난 **팀을 해체·정리**하는 도구. "세션당 한 팀, 종료 시 shutdown 필수"가 권장 규칙입니다.

<a id="g-p2p"></a>
- **peer-to-peer(P2P, 피어 투 피어)** — 중앙(리더)을 거치지 않고 **동료끼리 직접 통신**하는 방식. 팀원(피어)들이 서로 메시지를 주고받는 구조를 가리킵니다.

<a id="g-eventloop"></a>
- **이벤트 루프(event loop)** — 리더가 직접 일하지 않고 **들어오는 이벤트에 반응해 조율만 하는 실행 모델**. "리더는 이벤트 루프, 팀원은 피어"라는 표현이 여기서 나옵니다.

<a id="g-protocol"></a>
- **팀 통신 프로토콜(team communication protocol)** — 에이전트 파일에 명시하는, **팀원과 어떤 형식·규칙으로 메시지를 주고받을지** 정한 약속(예: JSON 메시지 스키마).

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 메타스킬과 파이프라인

<a id="g-metaskill"></a>
- **메타스킬 / 메타 하네스(meta-skill / meta-harness)** — **하네스를 만들어내는 하네스**. 새 작업이 주어지면 도메인을 분석하고 팀·에이전트·스킬을 스스로 설계해 산출하는 상위 스킬입니다.

<a id="g-pipeline6"></a>
- **6단계 파이프라인** — 메타스킬이 하네스를 만들어내는 절차: **① 도메인 분석 → ② 팀 아키텍처 설계 → ③·④ 에이전트·스킬 생성 → ⑤ 오케스트레이션 → ⑥ 검증** (앞뒤로 Phase 0·7의 순환 구조).

<a id="g-phase"></a>
- **Phase(페이즈)** — 워크플로를 구성하는 **단계**. 각 페이즈마다 활성화되는 에이전트와 목적이 달라집니다.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 아키텍처 패턴 (6종)

<a id="g-pat-pipeline"></a>
- **파이프라인(pipeline)** — 에이전트를 **일렬로 이어** 앞 결과가 뒤의 입력이 되는 패턴. 순서 자체가 의미를 만듭니다.

<a id="g-pat-fan"></a>
- **팬아웃·팬인(fan-out / fan-in)** — **같은 입력을 여러 에이전트에 병렬로 뿌리고(fan-out)** 결과를 **하나로 모으는(fan-in)** 패턴. 같은 대상을 여러 관점으로 동시에 검토할 때 씁니다.

<a id="g-pat-expert"></a>
- **전문가 풀(expert pool)** — **라우터가 요청을 분류해 알맞은 전문가 에이전트에게 배분**하는 패턴.

<a id="g-pat-genver"></a>
- **생성-검증(generator-verifier)** — 한 에이전트가 **만들고(generator)** 다른 에이전트가 **검사하며(verifier)**, 통과할 때까지 다시 만드는 루프. 보통 재시도 상한과 에스컬레이션을 둡니다.

<a id="g-pat-supervisor"></a>
- **감독자(supervisor)** — **런타임에 작업을 동적으로 나눠 배분**하는 패턴. 미리 정한 순서가 아니라 상황에 따라 분배가 달라집니다.

<a id="g-pat-hier"></a>
- **계층적 위임(hierarchical delegation)** — **총괄 → 팀장 → 실무자**로 이어지는 다층 구조. 큰 작업을 계층적으로 쪼개 내려보냅니다.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 운영·진화

<a id="g-drift"></a>
- **drift(드리프트)** — 문서·설정과 실제 동작이 **시간이 지나며 서로 어긋나는 현상**. 변경 이력을 물리적 증거로 남겨 이를 방지합니다.

<a id="g-rerun"></a>
- **재실행 / 부분 재실행(re-run / partial re-run)** — 하네스 **전체를 다시 돌리거나, 바뀐 일부 단계만 골라 다시 돌리는** 것.

<a id="g-adr"></a>
- **ADR(Architecture Decision Record)** — **아키텍처 결정 기록**. 왜 이런 구조를 택했는지 결정과 근거를 문서로 남기는 관행입니다.

<a id="g-btd"></a>
- **Build to Delete** — 처음부터 **버릴 것을 전제로 가볍게 만드는** 접근. 하네스를 영구 자산이 아니라 필요하면 갈아치우는 것으로 다룹니다.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)

### 실전편 용어

<a id="g-pr"></a>
- **PR 리뷰(Pull Request review)** — 코드 변경 요청(PR)의 **diff를 검토**하는 작업. 정적 분석·보안·테스트 영향 등을 여러 리뷰어가 나눠 봅니다.

<a id="g-diff"></a>
- **diff(디프)** — 코드의 **변경 전후 차이**. 리뷰·분석의 기본 입력 단위입니다.

<a id="g-migration"></a>
- **마이그레이션(migration) / 배치(batch)** — 레거시 코드·테스트를 **새 방식으로 옮기는 작업**(마이그레이션)과, 그 대상을 의존 관계에 따라 **묶은 실행 단위**(배치).

<a id="g-rca"></a>
- **RCA(Root Cause Analysis, 근본 원인 분석)** — 버그·장애의 **표면 증상이 아닌 근본 원인**을 찾아내는 분석. "로컬에선 되는데 왜 안 되나" 같은 재현·원인 규명 작업이 여기에 해당합니다.

<a id="g-blameless"></a>
- **blameless(무비난) post-mortem** — 사후 분석에서 **개인 책임을 묻지 않고 구조·프로세스 개선에 집중**하는 방식.

<a id="g-token"></a>
- **토큰 경제(token economy)** — LLM이 처리하는 **토큰(비용·컨텍스트 한도)을 자원으로 보고 아껴 쓰는 관점**. 무엇을 항상 로드하고 무엇을 필요할 때만 불러올지 설계합니다.

<a id="g-refarch"></a>
- **레퍼런스 아키텍처(reference architecture)** — 실전에서 그대로 참고할 수 있는 **표준 구조 청사진**.

[↑ 목차](#하네스엔지니어링-with-claude-code--목차)
